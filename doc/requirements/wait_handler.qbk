[section:wait_handler Wait handler requirements]

A wait handler must meet the requirements for a [link requirements.handler
handler]. A value `h` of a wait handler class should work correctly in the
expression `h(ec)`, where `ec` is an lvalue of type `const error_code`.

[endsect]
