[section:write_handler Write handlers]

A write handler must meet the requirements for a [link requirements.handler
handler]. A value `h` of a write handler class should work correctly in the
expression `h(ec, s)`, where `ec` is of type `const error_code` and `s` is of
type `const size_t`.

[endsect]
